#![allow(unused_variables)] // TODO(cleanup): Remove
#![allow(unused)] // TODO(cleanup): Remove

mod actions;
mod character;
mod effects;
mod goal;
mod item;
mod map;
mod menus;
mod render;
mod world;

mod typed_id;

fn main() {
    println!("Hello, world!");

    /*
    let mut x: f32 = 0;
    loop {
        let mut ix: u128 = x.round();
        x += 1;
        ix += 1;
        if x.round() != ix {
            println!("{x} != {ix}");
            break;
        }
    }
    */
}
// TODO: Implement main
